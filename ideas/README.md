# Ideas

Scratchpad for project/concept ideas. Each file is a functional sketch — what it does, not how to build it.

Run `/idea` in an omp session to capture a new one (prompt: `~/.omp/agent/prompts/idea.md`).
