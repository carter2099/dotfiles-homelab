# Failure email (rate-limited to one per unit per hour by notify-failure.sh).
# Drop-in because the base unit is installed by the app repo's release script.
[Unit]
OnFailure=notify-failure@%n.service
